# Instructions to run the app

- Install dependencies

  ```
    npm i
  ```

- Run the react app

  ```
    npm run dev
  ```

  - Run the Express server

  ```
    npm run server
  ```

### You are done
